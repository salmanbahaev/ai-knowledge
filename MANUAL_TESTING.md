# 🚀 AI Knowledge Platform - Инструкция по тестированию

## 📋 Требования

- Python 3.13+
- Docker Desktop
- Git

## 🔧 Запуск проекта

### 1. Запуск инфраструктуры

```bash
# Запустить PostgreSQL и Redis
docker-compose -f docker-compose-minimal.yml up -d
```

### 2. Запуск Backend

```bash
cd backend
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 3. Запуск Frontend (опционально)

```bash
cd frontend
npm start
```

---

## 🧪 ЭТАП 3.1: ТЕСТИРОВАНИЕ DOCUMENT MANAGEMENT

### ✅ **Базовая проверка системы**

1. **Проверьте health check:**

   - Откройте: http://localhost:8000/api/v1/health/
   - Ожидаемый результат: все сервисы "healthy"

2. **Откройте Swagger UI:**
   - Откройте: http://localhost:8000/docs
   - Должны видеть все API endpoints

---

### 📄 **Тестирование управления документами**

#### **A. Загрузка документа**

1. **Откройте Swagger UI:** http://localhost:8000/docs
2. **Найдите секцию:** "📄 Управление документами"
3. **Нажмите на:** `POST /api/v1/documents/upload`
4. **Нажмите:** "Try it out"
5. **Заполните поля:**
   - **file:** Выберите любой файл (PDF, TXT, DOCX, до 10MB)
   - **title:** "Мой тестовый документ"
   - **description:** "Описание для тестирования"
   - **is_sensitive:** false
6. **Нажмите:** "Execute"

**✅ Ожидаемый результат:**

```json
{
  "message": "Document uploaded successfully",
  "data": {
    "document_id": "uuid-string",
    "filename": "ваш-файл.pdf",
    "title": "Мой тестовый документ",
    "file_size": 12345,
    "status": "ready"
  }
}
```

#### **B. Просмотр документов**

1. **Нажмите на:** `GET /api/v1/documents/`
2. **Нажмите:** "Try it out" → "Execute"

**✅ Ожидаемый результат:**

- Список загруженных документов
- Информация о пагинации
- Метаданные каждого документа

#### **C. Получение конкретного документа**

1. **Скопируйте document_id** из предыдущего шага
2. **Нажмите на:** `GET /api/v1/documents/{document_id}`
3. **Вставьте document_id** в поле
4. **Нажмите:** "Execute"

**✅ Ожидаемый результат:**

- Полная информация о документе
- Все метаданные
- Статистика просмотров

#### **D. Скачивание файла**

1. **Нажмите на:** `GET /api/v1/documents/{document_id}/download`
2. **Вставьте тот же document_id**
3. **Нажмите:** "Execute"

**✅ Ожидаемый результат:**

- Файл должен скачаться
- Содержимое должно совпадать с оригиналом

#### **E. Статистика хранилища**

1. **Нажмите на:** `GET /api/v1/documents/stats/storage`
2. **Нажмите:** "Execute"

**✅ Ожидаемый результат:**

```json
{
  "message": "Storage statistics retrieved successfully",
  "data": {
    "document_count": 1,
    "total_size_bytes": 12345,
    "total_size_human": "12.1 KB",
    "ready_count": 1,
    "processing_count": 0,
    "error_count": 0
  }
}
```

#### **F. Обновление документа**

1. **Нажмите на:** `PUT /api/v1/documents/{document_id}`
2. **Вставьте document_id**
3. **В Request body введите:**

```json
{
  "title": "Обновленный заголовок",
  "description": "Новое описание",
  "is_sensitive": true
}
```

4. **Нажмите:** "Execute"

**✅ Ожидаемый результат:**

- Документ обновлен
- Новые метаданные сохранены

#### **G. Проверка целостности**

1. **Нажмите на:** `POST /api/v1/documents/{document_id}/verify`
2. **Вставьте document_id**
3. **Нажмите:** "Execute"

**✅ Ожидаемый результат:**

```json
{
  "data": {
    "document_id": "uuid",
    "is_valid": true,
    "status": "valid"
  }
}
```

#### **H. Удаление документа (мягкое)**

1. **Нажмите на:** `DELETE /api/v1/documents/{document_id}`
2. **Вставьте document_id**
3. **Оставьте hard_delete = false**
4. **Нажмите:** "Execute"

**✅ Ожидаемый результат:**

- Документ помечен как удаленный
- Файл остается в хранилище

---

### 🧪 **Дополнительные тесты**

#### **Тест фильтрации:**

1. Загрузите несколько документов разных типов
2. Используйте `GET /documents/` с параметрами:
   - `type=pdf`
   - `status=ready`
   - `search=тест`

#### **Тест пагинации:**

1. Загрузите много документов
2. Тестируйте `page=1&per_page=5`

#### **Тест больших файлов:**

1. Попробуйте загрузить файл > 10MB
2. Должна быть ошибка валидации

---

### 🔍 **Проверка файловой системы**

После загрузки документов проверьте:

1. **Папка storage создана:**

   ```
   backend/storage/
   ├── documents/
   ├── temp/
   ├── previews/
   └── chunks/
   ```

2. **Файлы зашифрованы:**

   - Откройте файлы в `backend/storage/documents/`
   - Содержимое должно быть нечитаемым (зашифровано)

3. **Ключ шифрования создан:**
   - Файл `backend/storage/.encryption_key` существует

---

## ❌ **Возможные проблемы и решения**

### **Ошибка "Upload failed":**

- Проверьте, что Docker контейнеры запущены
- Убедитесь, что база данных инициализирована
- Проверьте логи backend в консоли

### **Ошибка 404 при скачивании:**

- Файл может быть не найден в storage
- Проверьте права доступа к папке storage

### **Ошибка валидации файла:**

- Проверьте размер файла (макс. 10MB)
- Убедитесь, что тип файла поддерживается

---

## 🎯 **Критерии успешного тестирования**

✅ **Все endpoints отвечают без ошибок**  
✅ **Файлы загружаются и сохраняются**  
✅ **Метаданные корректно отображаются**  
✅ **Файлы можно скачать без повреждений**  
✅ **Шифрование работает (файлы нечитаемы)**  
✅ **Статистика показывает правильные данные**  
✅ **CRUD операции работают**  
✅ **Валидация файлов функционирует**

---

## 📊 **Текущий статус проекта**

✅ **Этап 1-2:** Frontend + Backend с базовыми API endpoints  
✅ **Этап 3.1:** Document Storage Foundation - **ЗАВЕРШЕН**  
🔄 **Следующий:** Этап 3.2 - Basic File Processing (text extraction)

---

**После успешного тестирования всех пунктов Этап 3.1 считается полностью завершенным!** 🚀
