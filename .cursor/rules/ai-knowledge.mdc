---
description: AI-Knowledge Graph Platform - Development Rules & Best Practices
globs: ["**/*"]
alwaysApply: true
---

# 🚀 AI-Knowledge Graph Platform - Development Rules & Best Practices

## 📋 АРХИТЕКТУРНЫЕ ПРИНЦИПЫ

### 🏗️ Модульность и Разделение Ответственности

- **Single Responsibility Principle**: Один класс/функция = одна ответственность
- **Dependency Injection**: Внедрение зависимостей через конструкторы/параметры
- **Interface Segregation**: Мелкие, специфичные интерфейсы вместо больших
- **Composition over Inheritance**: Предпочитать композицию наследованию
- **Domain-Driven Design**: Структура кода отражает бизнес-логику

### 🔧 Файловая Структура

```
src/
├── components/           # Переиспользуемые UI компоненты
│   ├── common/          # Базовые компоненты (Button, Input, Modal)
│   ├── layout/          # Layout компоненты (Header, Sidebar, Footer)
│   └── features/        # Специфичные компоненты функций
├── pages/               # Страницы приложения
├── hooks/               # Custom React hooks
├── services/            # API сервисы и бизнес-логика
├── store/               # State management (Zustand/Redux)
├── types/               # TypeScript типы и интерфейсы
├── utils/               # Утилитарные функции
├── constants/           # Константы приложения
└── assets/              # Статические ресурсы
```

## 🌍 INTERNATIONALIZATION (i18n)

### 📝 Language Support

- **Supported Languages**: English (en) and Russian (ru)
- **Default Language**: English with automatic browser detection
- **Storage**: Language preference saved in localStorage
- **Library**: react-i18next with browser language detector

### 🔧 Implementation Details

```typescript
// Translation structure
{
  "navigation": { "dashboard": "Dashboard" },
  "common": { "loading": "Loading..." },
  "dashboard": { "title": "Dashboard" }
}

// Component usage
const { t, i18n } = useTranslation('common');
<h1>{t('dashboard.title')}</h1>
```

### 🎨 Responsive Design for Languages

- **CSS Classes**: `i18n-*` classes for language-specific styling
- **Data Attributes**: `data-lang` for language-specific CSS rules
- **Adaptive Layout**: Russian text ~20-30% longer than English
- **Button Sizing**: `min-width` adjustments for different languages

### ✅ i18n Best Practices

- All user-facing text must use translations
- Placeholder text, error messages, navigation - everything translated
- Layout tested for both languages to prevent text overflow
- Language switcher in header with flags and native language names
- Responsive grid adjustments for longer text in Russian

## 💻 FRONTEND BEST PRACTICES

### ⚡ Производительность

- **Code Splitting**: React.lazy() + Suspense для ленивой загрузки
- **Мемоизация**: React.memo, useMemo, useCallback для оптимизации рендеров
- **Virtualization**: react-window для больших списков
- **Bundle Analysis**: webpack-bundle-analyzer для контроля размера
- **Tree Shaking**: ES6 imports для удаления неиспользуемого кода

### 🔒 TypeScript Standards

```typescript
// ✅ Хорошо - строгие типы для API
type ApiResponse<T> = {
  data: T;
  status: "success" | "error";
  message?: string;
};

// ✅ Generic компоненты
interface TableProps<T> {
  data: T[];
  columns: ColumnConfig<T>[];
  onRowClick: (item: T) => void;
}
```

### 🎨 UI/UX Excellence

- **Design System**: Консистентные цвета, типографика, отступы
- **Accessibility**: ARIA labels, keyboard navigation, screen readers
- **Responsive Design**: Mobile-first подход, fluid typography
- **Loading States**: Скелетоны, спиннеры, прогресс-бары
- **Error Boundaries**: Graceful error handling с fallback UI
- **Dark Mode**: Системная поддержка темной темы

## 🖥️ BACKEND BEST PRACTICES

### 🏛️ API Design

```python
# ✅ RESTful endpoints
GET    /api/v1/documents          # Список документов
GET    /api/v1/documents/{id}     # Конкретный документ
POST   /api/v1/documents          # Создание документа
PUT    /api/v1/documents/{id}     # Полное обновление
PATCH  /api/v1/documents/{id}     # Частичное обновление
DELETE /api/v1/documents/{id}     # Удаление

# ✅ Consistent response format
{
  "data": {},
  "meta": {
    "total": 100,
    "page": 1,
    "per_page": 20
  }
}
```

### 🔐 Security First

- **Input Validation**: Pydantic models для валидации всех входных данных
- **Rate Limiting**: Защита от DDoS и брутфорса
- **CORS Configuration**: Строгая настройка allowed origins
- **SQL Injection Protection**: Только параметризованные запросы
- **Authentication**: JWT + refresh tokens, secure httpOnly cookies
- **Authorization**: RBAC с детализированными правами

### 🚀 Performance & Scalability

- **Caching Strategy**: Redis для session, query results, rate limiting
- **Database Connection Pooling**: Async pool с настройкой min/max connections
- **Background Tasks**: Celery для длительных операций
- **Monitoring**: Prometheus metrics, structured logging
- **Health Checks**: Liveness/readiness endpoints для Kubernetes

## 🚨 CRITICAL RULES

### ❌ ЗАПРЕЩЕНО

- Любой код без TypeScript типов
- Мутация состояния напрямую (только через actions)
- Inline styles (только CSS-in-JS или CSS modules)
- Hardcoded URLs и константы
- Игнорирование ESLint/TypeScript ошибок
- Console.log в production коде

### ✅ ОБЯЗАТЕЛЬНО

- Code review для всех PR
- Unit тесты для всех сервисов и утилит
- Integration тесты для API endpoints
- TypeScript strict mode без any
- Structured logging с correlation IDs
- Performance budget: < 3s FCP, < 5s LCP

## 📝 CODE STANDARDS

### Naming Conventions

```typescript
// ✅ Components: PascalCase
const DocumentUploader: React.FC = () => {};

// ✅ Hooks: camelCase с префиксом use
const useDocumentUpload = () => {};

// ✅ Constants: SCREAMING_SNAKE_CASE
const MAX_FILE_SIZE = 10 * 1024 * 1024;

// ✅ Functions: camelCase, descriptive
const validateEmailFormat = (email: string): boolean => {};
```

### Error Handling

```typescript
// ✅ Typed errors
class ValidationError extends Error {
  constructor(public field: string, public value: any, message: string) {
    super(message);
    this.name = "ValidationError";
  }
}

// ✅ Result pattern
type Result<T, E = Error> =
  | { success: true; data: T }
  | { success: false; error: E };
```

**🎉 REMEMBER: Great code is not just working code - it's maintainable, testable, secure, performant, and delightful to work with!**
